/*
 * <%= title %> Assistant
 */

var <%= title %>Assistant = function () {};

<%= title %>Assistant.prototype = {
    setup: function () {
    },

    activate: function (event) {
    },

    deactivate: function (event) {
    },

    cleanup: function (event) {
    }
};